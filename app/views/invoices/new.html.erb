<main class="lcars-container">
  <%= turbo_frame_tag "invoice_payment" do %>

    <div class="lcars-subtitle">
      <h3><%= t('invoice.pay_invoice') %></h3>
    </div>

    <%= render "form", card_id: @card_id, accounts: @accounts  %>
  <% end %>
</main>
