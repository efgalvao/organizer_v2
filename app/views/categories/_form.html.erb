<%= simple_form_for category, html: { class: "category form" } do |f| %>
  <%= form_error_notification(category) %>

  <%= f.input :name, label: t('category.form.name'), input_html: { autofocus: true } %>
  <%= f.submit t('buttons.save'), class: "btn btn--secondary" %>
  <%= link_to t('buttons.cancel'), categories_path, class: "btn btn--light" %>
<% end %>
