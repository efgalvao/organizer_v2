<main class="container">
  <%= turbo_frame_tag "new_transaction" do %>

    <div class="lcars-subtitle">
      <h3><%= t('transactions.index.new_transaction')%></h3>
    </div>

    <%= render "form", transaction: @transaction, categories: @categories %>
  <% end %>
</main>
