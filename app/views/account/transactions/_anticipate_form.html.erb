<%= simple_form_for :anticipate_transaction, url: anticipate_account_transaction_path(transaction.account, transaction), method: :post, html: { class: "form" } do |f| %>
  <%= form_error_notification(transaction) %>

  <%= f.hidden_field :id, value: transaction.id %>
  <%= f.input :anticipate_date, as: :date, html5: true, label: t('transactions.form.anticipate_date') %>

  <%= f.submit t('buttons.anticipate'), class: "lcars-button lcars-button--secondary" %>
  <%= link_to t('buttons.cancel'), account_transactions_path(transaction.account), class: "lcars-button lcars-button--danger" %>
<% end %>
