<%= turbo_stream.update "new_financing_payment", "" %>

  <%= turbo_stream.prepend "payments" do %>
    <%= render partial: 'financing/payments/payment', object: @payment, locals: { financing: @financing } %>
  <% end %>

<%= render_turbo_stream_flash_messages %>
