<%= simple_form_for transference, html: { class: "transference form" }, data: { turbo: false }  do |f| %>
  <%= form_error_notification(transference) %>

  <%= f.input :value_cents, as: :float, label: "Valor", input_html: { autofocus: true } %>
  <%= f.input :date, as: :date, html5: true, label: "Data da transferência" %>
  <%= f.input :sender_id, as: :select, collection: all_accounts(current_user.id), label: "De" %>
  <%= f.input :receiver_id, as: :select, collection: all_accounts(current_user.id), label: "Para" %>
  <%= link_to "Cancel", transferences_path, class: "btn btn--light" %>
  <%= f.submit class: "btn btn--secondary" %>
<% end %>
